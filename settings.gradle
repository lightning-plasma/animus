pluginManagement {
  resolutionStrategy {
    eachPlugin {
      if (requested.id.id.startsWith("org.jetbrains.kotlin")) {
        useVersion("${kotlinVersion}")
      }
    }
  }
}

rootProject.name = "animus"

